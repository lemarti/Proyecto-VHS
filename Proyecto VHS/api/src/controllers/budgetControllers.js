/*requiero de precio y cantidad calcular total
*/
const sum = async (req, res) => {

    try {


    } catch (error) {
        console.log(error)

    }
}
/* una funcion para sumar y restar cantidad de productos
requiero cant, producto y tipo de operación */
const products = async (req, res) => {
    let { product, amount, operator } = req.body;

    if (product && amount && operator) {
        try {

        } catch (error) {
            console.log(error)

        }
    }
}